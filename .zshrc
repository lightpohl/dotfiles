export GITHUB_TOKEN="add token here"
export GIANTBOMB_TOKEN="add token here"

alias sudo="sudo "
alias ls="ls -A1 -G"

ZSH_THEME="avit"
ZSH_TMUX_AUTOSTART=false

plugins=(
  docker
  git
  npm
  nvm
  tmux
 )  
